{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/LatentCast/latentcast-skills/skills/find-contacts/references/output-schema.json",
  "title": "find-contacts record",
  "description": "One record per company, always emitted, so a company that returned nothing is distinguishable from one that was never run.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "company",
    "status",
    "people",
    "researched_at"
  ],
  "properties": {
    "company": {
      "type": "string",
      "description": "Company name as written."
    },
    "domain": {
      "type": "string",
      "description": "Registrable domain, no scheme. Used for deduplication."
    },
    "country": {
      "type": "string"
    },
    "industry": {
      "type": "string"
    },
    "audience": {
      "type": "string",
      "description": "Free text, matched downstream against offer.variants[].audience in the seller profile. Absent means the default offer applies."
    },
    "status": {
      "enum": [
        "ok",
        "partial",
        "failed"
      ],
      "description": "ok = target met. partial = fewer people or signals than asked for. failed = nothing usable. A failed record is still a record."
    },
    "notes": {
      "type": "string",
      "description": "Why a record is partial or failed, in one line."
    },
    "researched_at": {
      "type": "string",
      "description": "Date the research ran, YYYY-MM-DD. Taken from the environment at run start, never hardcoded."
    },
    "recency_window_months": {
      "type": "integer",
      "description": "What 'recent' meant for this run, so a later reader can judge staleness."
    },
    "people": {
      "type": "array",
      "description": "Verified decision-makers. May be empty; an empty array with status partial or failed is an honest answer.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "full_name",
          "title",
          "linkedin_url",
          "profile_source",
          "confidence",
          "persona_match",
          "why_right_contact"
        ],
        "properties": {
          "recipient_id": {
            "type": "string",
            "description": "Stable id, minted here as R-0001 upward. The resume key for every downstream step."
          },
          "full_name": {
            "type": "string"
          },
          "first_name": {
            "type": "string"
          },
          "last_name": {
            "type": "string"
          },
          "title": {
            "type": "string"
          },
          "profile_source": {
            "type": "string",
            "description": "Where the role was actually confirmed: 'linkedin', or a company leadership page, dated press release or speaker page if you corroborated there."
          },
          "confidence": {
            "enum": [
              "high",
              "medium",
              "low"
            ]
          },
          "persona_match": {
            "enum": [
              "strong",
              "adjacent",
              "fallback"
            ],
            "description": "Honest self-report against the declared buyer. Sorting a finished run by fallback is how you discover your buyer definition was too vague."
          },
          "why_right_contact": {
            "type": "string",
            "description": "Which declared title rule or persona clause this person satisfies. 'Senior leader at the company' is a failure, not a justification."
          },
          "role_status": {
            "enum": [
              "confirmed",
              "conflicting",
              "unconfirmed"
            ],
            "description": "conflicting means two sources disagree. Flag it rather than silently picking one."
          },
          "role_source_url": {
            "type": "string",
            "description": "The dated primary source, where one exists."
          },
          "primary": {
            "type": "string",
            "description": "yes/no. The main contact at this company."
          },
          "named_by_customer": {
            "type": "string",
            "description": "yes/no. Supplied by the customer rather than found; changes how much verification is owed."
          },
          "flags": {
            "type": "string",
            "description": "Data caveats worth carrying forward."
          },
          "linkedin_url": {
            "type": "string",
            "description": "The person's LinkedIn profile. The primary identifier, and what downstream tools key on."
          },
          "corroborated_by": {
            "type": "string",
            "description": "A second page you actually fetched that shows them in the role. LinkedIn blocks automated fetches, so a profile found through a search index is a cached view, which is exactly what preserves a title after someone has left. Corroborate, or cap confidence at medium."
          }
        }
      }
    },
    "evidence_url": {
      "type": "string",
      "description": "Sourcing step only. A page that names this company. Required there; a candidate without one is dropped."
    },
    "fit_score": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Sourcing step only. Judged from the company's own words, not a directory blurb."
    },
    "fit_reason": {
      "type": "string",
      "description": "Sourcing step only. One line, in their own words."
    },
    "liveness": {
      "enum": [
        "verified-200",
        "verified-403-waf",
        "dns-only-needs-check",
        "domain-unresolved"
      ],
      "description": "A label, never a reason to drop."
    },
    "site_count": {
      "type": "integer",
      "description": "Sourcing step. How many register entries this one organisation covers. Registers list premises; you are selling to the organisation."
    },
    "registers": {
      "type": "string",
      "description": "Sourcing step. Which register or registers listed this organisation. Two independent registers is a stronger signal than one."
    },
    "hq": {
      "type": "string",
      "description": "City or region."
    },
    "segment": {
      "type": "string"
    },
    "staff": {
      "type": [
        "integer",
        "string"
      ],
      "description": "Headcount or band. Load-bearing: below roughly fifty most functions collapse into the owner."
    },
    "company_fit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Durable. Does this company match the ICP at all."
    },
    "campaign_relevance": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Per-campaign. 3 when no campaign block is configured."
    },
    "score": {
      "type": "integer",
      "minimum": 1,
      "maximum": 5,
      "description": "Combined. Travels with every downstream row; not a filter."
    },
    "routing": {
      "type": "string",
      "description": "Derived from score against the profile's thresholds. Typically white-glove, cold-outreach or drop."
    },
    "open_items": {
      "type": "array",
      "description": "Things a human must decide before a send.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "item",
          "affects",
          "status",
          "detail"
        ],
        "properties": {
          "item": {
            "type": "string"
          },
          "affects": {
            "type": "string"
          },
          "status": {
            "enum": [
              "blocks the send",
              "needs a call",
              "check before use"
            ]
          },
          "detail": {
            "type": "string"
          }
        }
      }
    }
  }
}
